#include <stdio.h>
#include <stdlib.h>

int main()
{
	int c;
	FILE *n, *out;

	in = fopen(*file.in, "r");
	out = fopen("file.out", "w");

	while((c = fgetc(in)) != EOF)
		fputc(c, out);

		eixt(0);
}










